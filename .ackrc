# https://beyondgrep.com/documentation/ack-2.16-man.html

# Print a break between results from different files. This is the default when used interactively.
--break

# Show the column number of the first match. This is helpful for editors that can place your cursor at a given position.
--column

# Print a filename heading above each file's results. This is the default when used interactively.
--heading

# Sorts the found files lexicographically.
--sort-files

# Show more context around seach result
--context=7

--ignore-dir=.c9
--ignore-dir=.git
--ignore-dir=.idea
--ignore-dir=.webpack
--ignore-dir=node_modules